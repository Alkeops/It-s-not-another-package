useFactory:
  accounts:
    - public: ${PARENT_ACCOUNT_PUBLIC}
      secret: ${PARENT_ACCOUNT_SECRET}
      type: ISSUER
    - public: ${OWNER_ACCOUNT_PUBLIC}
      secret: ${OWNER_ACCOUNT_SECRET}
      type: OWNER
  account:
    parentAccount: OWNER
    startingBalance: '10'
    baseTrustline:
      - ${OTHER_ASSET}
  mode: TESTNET
